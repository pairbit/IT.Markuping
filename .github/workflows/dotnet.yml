name: .NET

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    name: test
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest, windows-11-arm, ubuntu-24.04-arm]

    steps:
      - uses: actions/checkout@v6
      - name: Setup
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: |
            3.1.x
            6.0.x
            7.0.x
            8.0.x
            10.0.x
    
      - name: Restore
        run: dotnet restore IT.Markuping.sln
    
      - name: Build
        run: dotnet build IT.Markuping.sln --configuration Release --no-restore 

      - name: Test
        run: dotnet test IT.Markuping.Tests/IT.Markuping.Tests.csproj --configuration Release --no-restore

  publish:
    needs: [build]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Publish IT.Markuping
        uses: pairbit/publish-nuget@v2.6.2
        with:
            PROJECT_FILE_PATH: IT.Markuping/IT.Markuping.csproj
            NUGET_KEY: ${{secrets.NUGET}}
            TAG_COMMIT: true
